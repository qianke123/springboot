server:
  port: 8081

spring:
  application:
    name: eureka-service # 微服务的名称
  activemq:
    broker-url: tcp://192.168.88.132:61616 # activeMq的服务地址
    user: admin
    password: admin
  jms:
    pub-sub-domain: false # false = Queue true = topic
# 自己定义的队列名称
myqueue: boot-activemq-queue

eureka:
  client:
    service-url:
      defaultZone: http://192.168.88.132:8080/eureka/ # 注册服务的地址